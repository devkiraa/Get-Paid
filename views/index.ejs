<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticketing</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex justify-center items-center h-screen">
    <div class="bg-white p-8 rounded shadow-md">
        <h1 class="text-xl font-bold mb-4">Kerala Transport Ticketing</h1>
        <form id="ticketForm">
            <div class="mb-4">
                <label class="block">From:</label>
                <select name="from" class="w-full p-2 border border-gray-300 rounded" required>
                    <!-- Add options dynamically -->
                </select>
            </div>
            <div class="mb-4">
                <label class="block">To:</label>
                <select name="to" class="w-full p-2 border border-gray-300 rounded" required>
                    <!-- Add options dynamically -->
                </select>
            </div>
            <div class="mb-4">
                <label class="block">Number of Passengers:</label>
                <input type="number" name="passengers" min="1" class="w-full p-2 border border-gray-300 rounded" required>
            </div>
            <div class="mb-4">
                <label class="block">Payment Method:</label>
                <select name="payment" class="w-full p-2 border border-gray-300 rounded">
                    <option value="UPI">UPI</option>
                    <option value="Mobile">Mobile</option>
                    <option value="Cash">Cash</option>
                </select>
            </div>
            <button type="submit" class="w-full bg-blue-500 text-white p-2 rounded">Submit</button>
        </form>
    </div>
</body>
</html>
